# Template for ~/.pocketdeck.env
# Copy to ~/.pocketdeck.env and `chmod 600` it.

export POCKETDECK_HOST=192.168.1.42
export POCKETDECK_USER=user
export POCKETDECK_PASSWORD=changeme
export POCKETDECK_REMOTE_DIR=/sd/py
